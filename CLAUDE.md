## Agent skills

### Issue tracker

Issues live in GitHub Issues on `amwebexpert/etoolbox`, via the `gh` CLI. See `documents/agents/issue-tracker.md`.

### Domain documents

Single-context layout — `CONTEXT.md` + `documents/adr/` at the repo root (created lazily, don't exist yet). See `documents/agents/domain.md`.
