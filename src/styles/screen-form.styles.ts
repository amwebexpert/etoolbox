import { createStyles } from "antd-style";

export const useScreenFormStyles = createStyles(() => ({
  fullWidth: {
    width: "100%",
  },
  form: {
    width: "100%",
  },
  formItem: {
    marginBottom: 16,
  },
  textArea: {
    fontFamily: "monospace",
  },
}));
