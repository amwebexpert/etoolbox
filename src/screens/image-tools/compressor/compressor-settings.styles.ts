import { createStyles } from "antd-style";

export const useStyles = createStyles(() => ({
  collapse: {
    width: "100%",
  },
  collapseLabel: {
    display: "flex",
    alignItems: "center",
    gap: 8,
  },
  form: {
    width: "100%",
  },
}));
